# sticky-dvr — Frontend (static SPA)
#
# Usage:
#   docker compose -f deploy/compose/frontend.yaml --env-file deploy/.env up -d

services:
  frontend:
    image: sticky-frontend:latest
    restart: unless-stopped
    networks:
      - sticky-dvr

networks:
  sticky-dvr:
    external: true
