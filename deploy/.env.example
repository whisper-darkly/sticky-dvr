# sticky-dvr deploy environment
# Copy to .env and fill in values before starting services.
# All services must be on the same Docker network:
#   docker network create sticky-dvr

# ---- Paths (bind mounts) ----
DATA_DIR=/opt/sticky-dvr                # base for per-service state dirs
RECORDINGS_DIR=/opt/sticky-dvr/recordings
CONFIG_DIR=/opt/sticky-dvr/config       # copy deploy/config/* here
CERTS_DIR=/opt/sticky-dvr/certs        # copy TLS certs here (tls.crt, tls.key)

# ---- PostgreSQL ----
POSTGRES_DB=sticky
POSTGRES_USER=sticky
POSTGRES_PASSWORD=changeme
PG_ADMIN_USER=postgres
PG_ADMIN_PASSWORD=changeme-admin
DB_DSN=postgres://sticky:changeme@postgres:5432/sticky?sslmode=disable

# ---- Auth ----
JWT_SECRET=change-me-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production

# ---- Service endpoints (container names = DNS hostnames) ----
OVERSEER_URL=ws://overseer:8080/ws
CONVERTER_URL=ws://converter:8080/ws

# ---- GPU (converter) ----
# Which GPU(s) to expose to the converter container.
# "all" = all GPUs; "0" = first GPU; "GPU-uuid" = specific GPU by UUID.
NVIDIA_VISIBLE_DEVICES=all
